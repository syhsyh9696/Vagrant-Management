<div id="follow_form">
<%= form_for current_user, :url => relationships_experiments_path, :html => {:method => :post}, :remote => true do |f| %>
  <div><%= hidden_field_tag :experiment_id, @experiment.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary btn-block" %>
<% end %>
</div>