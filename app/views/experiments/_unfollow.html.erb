<div id="follow_form">
<%= form_for current_user, :url => relationships_experiments_path, :html => {:method => :delete}, :remote => true do |f| %>
  <div><%= hidden_field_tag :experiment_id, @experiment.id %></div>
  <%= f.submit "Unfollow", class: "btn btn-primary btn-block" %>
<% end %>
</div>